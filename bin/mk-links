#!/bin/bash
# --------------------------------------------------------------
# Anlegen der Verweise fuer ankommende Daten 
# (c) 2013 IFAOE.DE A. Weidauer; weidauer@ifaoe.de
# --------------------------------------------------------------
IN_PATH=/home/census/hwi-2010/incoming
DT_PATH=/home/census/hwi-2010/2013-09-rebuild-awe/data
ln -s $IN_PATH/2013-09-uni-hro/exif $DT_PATH/exit-tif-raw 
ln -s $IN_PATH/2013-09-uni-hro/gps  $DT_PATH/gps-tracks 
ln -s $IN_PATH/2010-03-ifaoe/img-tif-raw $DT_PATH/img-tif-raw
ln -s $IN_PATH/2010-03-ifaoe/bird-census $DT_PATH/bird-census
# --------------------------------------------------------------
# EOF
# --------------------------------------------------------------
